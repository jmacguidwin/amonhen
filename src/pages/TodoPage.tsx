import React from "react";
import CameraLayout from "../components/components/layouts/CameraLayout";

type Props = {}

const Todopage = (props: Props) => {
    return (
        <CameraLayout>
            TodoPage
        </CameraLayout>
    )   
}

export default Todopage;